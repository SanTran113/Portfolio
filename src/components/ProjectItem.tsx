function ProjectItem({
    coverImage,
    name,
    typeOfWork,
    skillsUsed,
    link,
}: {
  coverImage?: string;
    name?: string;
    typeOfWork?: string;
    skillsUsed?: string[];
    link?: string;
}) {
  return <div>ProjectItem</div>;
}

export default ProjectItem;
